# CS207_Project

## 功能流程图

```mermaid
graph TD

A[主菜单]
B{**功能选择方案**：
1、使用拨码开关选择功能
2、采用UI按钮选择功能
}

C[矩阵输入与储存]
D[矩阵生成与储存]
E[矩阵展示]
F[矩阵运算]

A-->B
B-->C
B-->D
B-->E
B-->F

subgraph 用户可能的输入格式
e1[矩阵规模（x,y）+ 编号]
e2[矩阵规模（x,y）+ 矩阵数量]
e3[矩阵规模（x,y）+矩阵元素]
end

C--->e3
D--->e2
E-->e1
F-->e1

H[计算类型选择
使用拨码开关，要在七段数码管上显示计算类型
（按钮确认）]

F-->H

h1{矩阵加法}
h2[矩阵标量乘法]
h3[矩阵转置]
h4{矩阵乘法}
h5[矩阵卷积]

H-->h1
H-->h2
H-->h3
H-->h4
H-->h5

subgraph 单位选择模块
I[输入维度x]
J[输入维度y]
k[随机选取符合条件的运算数]
J-->k
K[输出格式：
编号+
矩阵元素]
L[输入编号]
M[判断合法性]
m[不合法时触发倒计时，用七段数码管显示]
I-->|按下按钮|J
J-->|按下按钮|K
K-->L
L-->|按下按钮|M
M-->m

end

h1-->I
h2-->I
h3-->I
h4-->I
h5-->I

subgraph 设置模块
N[同规模矩阵上限数量]
O[矩阵维度范围]
end

N-->A
O-->A

```



## 更新说明

把 top.v 作为主文件（只搭了框架），烧上去后打开 uart 通信。

五个拨码开关控制五种模式，拨动某一个再按中间按钮可以进入相应模式，会在数码管上和 uart 上都显示一遍，按 rst 才能回到 default 模式。

已经写好了 show 模式，轮流输入两个维度然后显示。目前并没有接入输入矩阵和生成矩阵的功能，所以现在显示的矩阵是我随便塞进去的，后面两个功能加了以后扔掉即可。

